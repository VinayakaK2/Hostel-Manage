# Base URL for API calls. In development, leave empty to use Vite proxy (/api -> backend).
VITE_API_BASE_URL=

# Optional: override proxy target when running `npm run dev`
# VITE_API_PROXY_TARGET=http://localhost:4000
